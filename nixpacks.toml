[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install -g pnpm@10.4.1",
  "npx --yes pnpm@10.4.1 install --frozen-lockfile"
]

[phases.build]
cmds = ["npx --yes pnpm@10.4.1 run build"]

[start]
cmd = "node dist/index.js"
